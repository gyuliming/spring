<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssg.webmvcboard.mapper.BoardMapper">

    <insert id="insert">
        insert into post_board(title, content, writer)
        values (#{title}, #{content}, #{writer})
    </insert>

    <select id="selectAll" resultType="com.ssg.webmvcboard.domain.BoardVO">
        select * from post_board order by tno desc
    </select>

    <select id="selectOne" resultType="com.ssg.webmvcboard.domain.BoardVO">
        select * from post_board where tno = #{tno} order by tno desc
    </select>

    <update id="updateViewCount">
        update post_board set viewCount = viewCount + 1 where tno = #{tno}
    </update>

    <update id="update">
        update post_board set title = #{title}, content = #{content} where tno = #{tno}
    </update>

    <delete id="delete">
        delete from post_board where tno = #{tno}
    </delete>

<!--    <select id="selectPassphrase" resultType="string">-->
<!--        select passphrase from post_board where tno = #{tno}-->
<!--    </select>-->

</mapper>
